interface:
  display_name: "Writing Audit"
  short_description: "Detailed prose audit with source fidelity"
  default_prompt: "Use $writing-audit to audit or rewrite this text with its detailed rulebook while preserving voice, facts, and protected content."
policy:
  allow_implicit_invocation: false

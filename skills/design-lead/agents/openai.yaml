interface:
  display_name: "Design Lead"
  short_description: "Design direction, references, execution, and critique"
  default_prompt: "Use $design-lead to guide this interface from goals and visual references through design decisions, implementation, and focused review."
